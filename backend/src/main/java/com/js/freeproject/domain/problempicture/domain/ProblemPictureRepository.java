package com.js.freeproject.domain.problempicture.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ProblemPictureRepository extends JpaRepository<ProblemPicture, Long> {
}
